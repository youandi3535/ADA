# 🎨 ADA 마이페이지 & 사용자 메뉴 기획서

> 2026-06-14 · 구글 로그인 v1 완료 후속 · **기획(설계)만, 구현 아님**
> 목표: "로그인 했다는 표시 + 사용자 메뉴 + 내 분석 기록"을 세계적 서비스 수준으로

---

## 0. 한눈에 — 무엇을 만드나

세 덩어리예요:

1. **로그인 표시 + 사용자 메뉴** — 우측 상단 프로필 아이콘(아바타) → 클릭하면 드롭다운(이름, 마이페이지, 로그아웃 등)
2. **마이페이지** — 내 프로필 + **내 분석 기록**(올린 파일, 돌린 분석, 결과물)
3. **(선행 작업) 분석을 사용자와 연결** — 지금 안 돼 있어서 이게 맨 먼저예요

---

## 1. 먼저, 질문 3개 답

**Q1. 가입/로그인은 DB에 연결돼 있지?**
→ ✅ **네.** `users`(회원 명부), `oauth_accounts`(구글 연동), `security_audit_log`(로그인 이력)에 다 저장돼요.

**Q2. 로그아웃도 DB에 연결돼야 하나?**
→ ✅ **백엔드 `/logout`은 이미 audit_log에 기록**하게 만들어져 있어요. **단, 지금 화면의 로그아웃 버튼은 세션만 지우고 그 API를 안 불러요.** 그래서 "로그아웃 시각까지 기록"하려면 버튼이 `/logout`을 호출하게 **한 줄만 연결**하면 돼요.
→ 실무에서도 로그아웃은 ① 보안 감사(언제 나갔나) ② 토큰 폐기를 위해 기록합니다.

**Q3. (숨은 질문) 분석 기록은 사용자와 연결돼 있나?**
→ ❌ **아니요. 이게 이번 기획의 핵심이에요.** (아래 2번)

---

## 2. ⚠️ 핵심 발견 — 지금 "분석"은 사용자와 연결돼 있지 않아요

마이페이지의 꽃은 "내 분석 기록"인데, 코드를 보니:

- 파일 업로드(`Upload`)·분석 작업(`Job`)을 만들 때 **누가 했는지(`user_id`)를 안 적어요.**
- 업로드/분석 API가 **로그인을 요구하지도 않아요.**

즉 지금은 "누구의 분석인지" 모르는 상태예요. **그래서 마이페이지를 만들려면 "분석 ↔ 사용자" 연결이 먼저**입니다.

다행히 — 테이블에는 `user_id` 칸이 **이미 있어요.** 채우기만 하면 됩니다.

| 항목 | 지금 상태 | 필요한 일 |
|---|---|---|
| users / oauth / 로그인이력 | ✅ 저장됨 | (없음) |
| 로그아웃 기록 | ⚠️ API는 있는데 화면이 안 부름 | 버튼 → `/logout` 연결 |
| uploads · jobs 테이블 | ✅ 존재 (user_id 칸 있음) | (없음) |
| 업로드·분석에 user_id 채우기 | ❌ 안 됨 | **인증 받아서 user_id 기록** |

---

## 3. 기능 A — 사용자 메뉴 (아바타 드롭다운)

### 세계 표준 패턴 (구글·GitHub·Notion이 다 똑같아요)

우측 상단 **원형 아바타** → 클릭하면 드롭다운이 펼쳐져요:

```
        (아바타 클릭)
              ▼
┌───────────────────────────┐
│ 🟢 홍길동                  │ ← 이름
│    hong@gmail.com         │ ← 이메일
├───────────────────────────┤
│ 👤  마이페이지             │
│ 📊  내 분석 기록           │
│ ⚙️   설정                  │
│ 🛡️   관리자 (권한 있을 때) │
├───────────────────────────┤
│ 🚪  로그아웃               │
└───────────────────────────┘
```

- 아바타 = **구글 프로필 사진**, 없으면 이름 첫 글자 동그라미(예: `홍`)
- 1초 안에 "내가 로그인돼 있구나 + 여기서 다 할 수 있구나"가 느껴지는 게 핵심

### 우리 적용

- 위치: 화면 우측 상단 (현재 Streamlit 구조상 사이드바 상단 또는 헤더 영역)
- 지금도 사이드바에 이메일·로그아웃이 있으니 **절반은 이미 있어요.** 이걸 아바타+드롭다운으로 다듬는 거예요.

---

## 4. 기능 B — 마이페이지

### 4-1. 프로필 영역
이름 · 이메일 · 가입일 · 권한(role) · 마지막 로그인 시각

### 4-2. ⭐ 내 분석 기록 (이게 핵심)

좋은 소식 — **데이터는 이미 다 DB에 있어요.** 사용자와 연결만 하면 바로 보여줄 수 있어요.

| 보여줄 것 | 데이터 출처(이미 존재) |
|---|---|
| 내가 올린 파일 | `uploads` |
| 내가 돌린 분석 | `jobs` |
| 결과물 (PPT·PDF·대시보드) | `outputs`, `artifacts` |
| 모델 성능·정확도 | `models` |

**분석 한 건 = 카드 하나로:**
```
┌──────────────────────────────────────┐
│ 📄 titanic.csv  ·  정형 ML 분석        │
│ 2026-06-14 14:30  ·  ✅ 완료          │
│ 정확도 94% · 핵심 피처 28개            │
│ [결과 다시 보기] [다운로드] [다시 실행] │
└──────────────────────────────────────┘
```

### 4-3. 설정
이름 변경 · 알림 · 연결된 계정 관리 · **회원 탈퇴(내 데이터 삭제)**

---

## 5. 세계적 기업은 어떻게 하나 (벤치마크)

| 서비스 | 마이페이지에 뭐가 있나 | 우리가 훔칠 점 |
|---|---|---|
| **Kaggle** | 내 노트북·데이터셋·대회기록·메달 | 활동을 "내 작품"으로 자랑스럽게 보여줌 |
| **Hugging Face** | 내 모델·데이터셋·Spaces·활동피드 | 결과물 중심으로 깔끔히 정리 |
| **Databricks** | 최근 노트북·Job 실행 이력·워크스페이스 | "최근 항목 + 재실행"을 전면에 |
| **GitHub** | 아바타 드롭다운(프로필·설정·로그아웃) | 메뉴 구조의 교과서 |
| **Google** | 아바타 → 계정 관리·다른 계정·로그아웃 | 누구나 1초 만에 이해 |

→ **공통 성공 공식 4가지:**
1. 우측 상단 **아바타 드롭다운** (예외 없음)
2. 활동을 **카드/리스트**로
3. **다시 보기 · 다운로드 · 재실행** (데이터 분석은 "재현성"이 생명)
4. **빈 화면도 예쁘게** (처음 온 사람에게 "첫 분석 시작하기" 안내)

---

## 6. 100점 체크리스트

- [ ] 아바타 드롭다운 (업계 표준 UX)
- [ ] 분석 기록 = 카드 + **다시보기/다운로드/재실행**
- [ ] **빈 상태 디자인** ("아직 분석이 없어요 → 지금 시작하기")
- [ ] 검색 · 필터 · 정렬 (날짜/종류/상태)
- [ ] 사용량 요약 ("이번 달 분석 12건" 같은 한 줄 통계)
- [ ] **본인 것만 보임** (다른 사람 분석 절대 안 보이게 — 권한 검사)
- [ ] 로그아웃 = audit 기록 + 토큰 폐기
- [ ] **회원 탈퇴 = 데이터 삭제권** (개인정보보호법/GDPR)
- [ ] 모바일·작은 화면에서도 안 깨짐

---

## 7. 단계별 로드맵 (작은 것부터)

### v1 — "로그인 티 내기" (지금 바로, 가치 큼)
- 사이드바에 **아바타 + 이름 + 로그아웃** (이미 절반 있음 → 다듬기)
- 로그아웃 버튼 → `/logout` API 연결 (**DB에 로그아웃 기록**)
- **분석 ↔ 사용자 연결** (선행): 업로드·분석 API가 로그인 받아 `user_id` 기록

### v2 — "마이페이지 본체"
- 마이페이지 화면 + **내 분석 기록(카드 목록)**
- 결과물 다시 보기 / 다운로드

### v3 — "100점"
- 검색·필터·정렬, 사용량 통계, 즐겨찾기, 재실행 버튼, 회원 탈퇴

---

## 8. 보안 · 프라이버시 (세계 기준)

- 🔒 **본인 데이터만** — 남의 분석은 절대 못 보게 (API에서 `user_id` 일치 검사 필수)
- 🔒 **회원 탈퇴 시** 데이터 삭제 또는 익명화 (개인정보보호법·GDPR의 "잊힐 권리")
- 🔒 민감정보(PII)는 마스킹 (이미 `redactor` 있음)
- 🔒 마이페이지 API도 로그인 토큰(JWT) 검사

---

## 9. 누가 구현하나

이 기능들은 `api` · `frontend` · `ada/db` 영역이라 **HJ 협의/구현**이 필요해요(로그인 작업과 같은 범위). 이 문서를 확정안으로 HJ에게 전달하면, 위 v1 → v2 → v3 순서로 진행할 수 있어요.

> 한 줄 요약: **데이터는 이미 다 있다. "분석을 사용자와 연결"만 해주면, 마이페이지는 그 위에 화면만 얹는 일이다.**
