# ADA 발표 PPT 구성 (v2 · 정석/실무 보정본)

> **목표 시간** 20~30분 · **청중** 수료 심사 + 외부 IT 초청자 · **발표** 1인
> **개발 기간** 2026.5.12 ~ 6.18 (약 5주)
> v1 합본을 **정석/실무 기준으로 사실·구조 보정**한 버전입니다. **목차는 v1과 동일하게 유지**하고, 각 슬라이드의 내용 정확도·중복·완성도만 손봤습니다.

---

## 0. 한눈에 — v2에서 바로잡은 것

**정확성·정직성 보정 (외부 IT 앞에서는 정직함이 곧 신뢰)**

- **에이전트 수 = 28개로 통일.** 코드 `agents/stubs.py`가 `len(ALL_AGENT_CLASSES) == 28` 을 강제합니다. v1은 본문 "27로 통일"·체크리스트 "28로 통일"로 서로 엇갈려 있었음 → **28로 일원화**. (발표 전 "28개 = 어떤 그룹 합인지" 1줄만 확정해 두면 깔끔)
- **KPI는 본문·부록 모두 "주요 4개 + 보조 메트릭"으로 통일.** v1 부록의 "KPI 11종"은 코드(`ada/observability/kpi.py`, 주요 4종 정의)와 맞지 않아 삭제·완화.
- **숫자 현행화:** DB 테이블 **27개**(초기 24 + 오토픽스·로그인 3), 컨테이너 **core 10개 기본**(선택 프로파일 포함 최대 15).
- **근거 약한 수치는 완화:** "보안 50종 0관통", "모델 19개", "2회차 +5%p" 등은 **실측 표기 또는 예시 제시**로 바꿔 과장 인상 제거.
- **강화학습은 "설계만, 향후"** 유지 (코드상 미구현 확인됨 — 정직성 보정).

**중복 제거·재배치**

- 트러블슈팅(10)에 남아 있던 **초안 메모·중복 문단 삭제** → 대표 사례 4개로 정리.
- 10번에 섞여 있던 **"향후 고도화" 로드맵 → 11.비전으로 이동** (목차는 그대로, 내용만 제자리로).
- 게이트 표기를 **"사용자 선택 5회(HITL 게이트)"** 기준으로 통일 (6·7번의 G 표기 불일치 해소).

---

## 1. 표지

- **제목** ADA — Adaptive Auto AI Pipeline Agent
- **카피** "3명이 3주 걸릴 프로젝트를 30분 만에"
- **팀** oneplus3 + 로고 (로고 별도 제작 가능)
- 팁: 카피 한 줄을 크게. 발표 시작과 동시에 **데모를 백그라운드로 미리 켜 두기**(예열).

---

## 2. 팀 소개 + 개발 기간

- **개발 기간** 2026.5.12 ~ 6.18 (약 5주)
- 팁: **30~45초** 안에 빠르게. 길게 끌면 초반 몰입이 깨짐.

> **인원·역할 정리(확인 필요):** 역할은 A~D 4개이지만 **실제 인원은 3명**입니다. 즉 1인이 인접 역할을 겸임하는 구조입니다. 아래 표는 **역할 기준**이며, 발표 전 **실제 인원-역할 매핑과 실명**을 확정하세요. (예: D의 평가/관측성·QA를 A 또는 C가 겸임)

| 역할 | 담당(실명 확정 필요) | 담당 에이전트 | 핵심 책임 |
|---|---|---|---|
| **A · Agent Lead + PO** (오케스트레이션·PM·UI) | 김효중 | Supervisor · Insight · Error Recovery | LangGraph 워크플로우 설계·구현 / 라우팅·HITL 게이트 / 공용 프롬프트 표준 / Streamlit UI·시각화 / 일정·통합테스트·데모 |
| **B · ML/Modeling Engineer** | _[팀원 2]_ | Model Selection · Training Executor · Training Monitor · Metrics Aggregator | 카테고리별 ML·DL·시계열 파이프라인 / 학습·모니터링·순위 / SHAP 해석 / MLflow / 모델 서빙 API |
| **C · Platform/Backend Engineer** | _[팀원 3]_ | Data Profiler · Schema Validator | DB 스키마 / FastAPI Gateway·인증·WebSocket / Celery+Redis 큐 / Docker Compose / 리눅스 서버 |
| **D · AI Eng. + Eval/QA** (겸임 가능) | _겸임 표기_ | Preprocessing Strategist · Feature Engineer · EDA · PPT Composer | 전처리·인코딩 / EDA 시각분석·PPT 템플릿 / 에이전트 평가 / 관측성(LangSmith·Langfuse) / 프롬프트 회귀 테스트 |

---

## 3. 목차

1. 표지
2. 조원 소개 + 개발 기간
3. 목차
4. 정의
5. 인프라 + 아키텍처
6. 대시보드 시연
7. 에이전트 파이프라인
8. 차별점
9. KPI
10. 트러블 슈팅
11. 비전
12. Q&A
13. 감사합니다!

---

## 4. 정의

**핵심 메시지:** "전공·비전공 상관없이, 누구나 몇 번의 클릭으로 전문가 수준 데이터 분석 + 자동 문서화."

**들어갈 내용 (3박자로 압축):**

1. **출발점 (실제 경험)** — 학원 발표용 PPT를 Gamma·Genspark·GPT 등으로 만들다가 → 퀄리티가 안 나오고, 좋은 건 비싸고, 프롬프트로 맞추자니 오래 걸림 → "에이전트로 직접 만들어 보자."
2. **두 번째 고통 (접근성)** — 비전공자로 데이터 분석을 배우며 "이 데이터로 뭘, 어떻게 해야 하지?"의 막막함을 직접 겪음.
3. **기존 방식의 한계** — 블랙박스(AutoML) · 전문가 전용 · LLM 할루시네이션(결과가 매번 다름).

→ **전환 한 줄:** "그래서 ADA를 만들었습니다 — 누구나 쉽고 빠르게 데이터를 분석해 인사이트를 자동 문서화하는 멀티 에이전트(**28개**)."

**멘트(추천):** "처음엔 PPT 만들기가 귀찮아서 시작했는데, 막상 보니 기존 데이터 분석은 AutoML식 블랙박스거나 전문가용이고, ChatGPT식은 결과가 매번 달랐습니다."

**슬라이드 팁:** 글 거의 없이. "며칠 걸리던 분석 → 30분" 한 줄(또는 이미지)만 강조. 차별점은 **한 줄만 예고**하고 "자세한 건 뒤에서"로 넘김.

---

## 5. 인프라 + 아키텍처

**핵심 메시지:** "화려한 기능보다 **토대(개발 환경)부터** 똑같이 깔았다."

**구축 순서 (이미지 1장 + 구두 설명):**
GitHub 연결 → Docker(동일 환경) → CI/CD(GitHub Actions + GHCR) → VPS 웹서버 → 로컬 백업 서버(Linux) → 자가학습용 KB 벡터 DB

**한 문장 요약:** "main에 합치면 CI/CD가 웹서버와 로컬서버에 **자동 배포**하고, 모두가 Docker로 **동일 환경**을 유지합니다."

**경험담 (이 슬라이드의 무기):** "그래서 다른 지역에 있는 조원도 재택으로 **완전히 같은 환경**에서 함께 개발할 수 있었습니다."

**팁:** 큰 그림 1장 + 30초로 짧고 굵게. 기술 스택은 부록으로 빼고, 이 그림 위에 **핵심 도구만** 얹기.

---

## 6. 대시보드 시연 (약 15분)

> ⏱️ **타이밍 핵심:** 데모 15분을 "가만히 보는 시간"으로 쓰지 말 것. 데모는 **백그라운드로 돌려두고**, 그 위에서 슬라이드 7~9를 설명하면서 선택 팝업이 뜰 때마다 화면으로 컷백. (데모 시간과 발표 시간이 **겹쳐야** 20~30분에 들어감)

**시연 흐름:**

- 대시보드 메인 → 파일 업로드 → 파이프라인 진행 (가로 전체 흐름 보여주기)
- 단계 사이 **팝업창 = 사용자 선택 구간**으로, **사용자 선택(HITL 게이트)은 총 5회**. 이때 대시보드 화면 ↔ PPT의 에이전트 파이프라인 화면을 **왔다갔다** 하며 설명.
- **분석 전반부 = 로컬 LLM(Ollama)** / **후반부 = Claude**
  - 이유: 로컬 Ollama는 PC 성능에 좌우되는데, 학원 PC 성능이 낮아 후반 단계를 감당하기 부족 → Claude로.
  - 이 **성능 이슈는 11.비전의 "강화학습 실제 구현"으로 연결**.

> ※ 게이트 번호 표기(G1~G5 등)는 **데모 화면의 실제 단계 라벨과 일치**시켜 확정하세요. 슬라이드 6·7에서 같은 번호 체계를 써야 청중이 헷갈리지 않습니다.

**팁:** 라이브 실패 대비 **전체 성공 녹화본 + 단계별 스크린샷**을 반드시 휴대(1인 발표라 더 중요).

---

## 7. 에이전트 파이프라인 (시연과 연결)

**핵심 메시지:** "**28개** 에이전트를 한 명도 빠짐없이 **오케스트라처럼** 지휘한다."

**설명 구조 (28개를 쉽게 푸는 법):**

- **오케스트라 + 공유 디스패처 + 카테고리 분배**로 설명.
- 4 카테고리: **정형 ML · 정형 DL · 시계열 · 이상탐지** (코드상 `pipelines/`에 4종 파이프라인 모두 구현됨).
- 공유 디스패처가 업로드된 데이터를 **맞는 카테고리로 라우팅** → 그 카테고리 로직이 돈다.
- 예시 데이터(레드 스레드): **타이타닉 = 정형 ML** → 업로드~산출물 흐름을 이 한 사례로 끝까지 따라감.

> ※ 기존 vs ADA "차별점 비교표"는 **8번(차별점)에 모았습니다.** 여기서는 파이프라인 구조에만 집중.

---

## 8. 차별점

**먼저 비교표로 전체 그림 (기존 vs ADA):**

| 차별점 | 기존 데이터분석 LLM·AI 에이전트 | ADA |
|---|---|---|
| HITL 게이트 | 완전 자동(블랙박스) 또는 완전 수동 | **5번만** 사람이 선택 + 근거 카드 제시 |
| 정직한 검증(누수 방지) | 결과를 그냥 내줌 | 누수 차단 + naive 못 이기면 탈락 + '분석 불가' 보고 |
| KB 자가학습 | 매번 LLM 호출, 기억 없음 | 팀 Q&A·분석 결과를 벡터 KB(pgvector 768차원)에 쌓아 재사용 |
| 하네스 엔지니어링 | 라이브러리 조합 — 터지면 멈춤 | LLM 단일 진입 · 예외→상태 변환, 안 멈춤 |
| 자동 오류 수정 | 에러는 사용자 몫 | 다단계(Tier) 자동 수정 + 샌드박스 검증 후 적용 |
| 5종 산출물 | 차트·노트북까지 | PPT · 발표대본(script) · HTML · 인사이트(markdown)까지 자동 |
| 강화학습 모델 | — | _설계는 완료, 현재 PC 성능 한계로 미구현 → 향후 과제_ |

**그다음 3가지를 깊이 있게 (각 ½~1장):**

1. **자가학습 (KB + 하네스 + 자가강화)**
   - KB: 과거 분석·해결책·팀 Q&A 저장 (pgvector 768차원).
   - 하네스: distiller(교훈 자동 증류) + RAG(의미 검색)로 적재·재사용.
   - 자가강화: 성공 지식 신뢰도↑ / 재실행 시 튜닝 trial 절감(내부 측정 기준) / 팀 Q&A→KB, **3-Tier(KB→Ollama→Claude)로 비용↓**.
   > 수치("2회차 +5%p, Optuna trial −30%")를 쓸 거면 **`kpi_measure.py` 실측치로 교체**하고 "내부 측정 기준" 한 줄을 덧붙일 것.

2. **자동 오류복구 (Guardian, 다단계 Tier)**
   - Tier 0 정적 룰 기반 분류·수정 → 에러 KB → 검증된 패치 재사용 → Ollama 코더 → Claude CLI.
   - **각 패치는 샌드박스 테스트를 통과해야만 적용.**
   - 보통 시스템은 오류=중단+사람 호출. ADA는 **자동 복구 + 그 해결책을 KB에 쌓아 재발 방지**.
   > v1의 "정적 패턴 6종"은 코드상 에러 분류 5종(+미분류)과 어긋남 → **"정적 룰 기반 분류"로 표현**하거나 실제 분류 개수로 정정.

3. **재현성/정직한 검증**
   - 최종 결정을 LLM에 맡기지 않음 — 합/불은 **임계값 규칙**, 1등 모델은 **실제 지표**로.
   - 데이터 누수 차단 (자세한 사례는 트러블슈팅에서).

**팁:** 깊이는 "개념 한 줄 + 흐름 한 줄"이면 충분. **강화학습은 솔직하게 "설계까지, 향후 구현"** 으로. (외부 IT는 과장보다 정직에 점수)

---

## 9. KPI

**구조:** ROI = 결론(헤드라인), KPI = 그걸 떠받치는 증거. **섞지 말 것** (라벨 분리).

**KPI — 주요 4개만 크게 (얼마나 잘 되나):**

| KPI | 무엇을 증명 | 측정(정직) |
|---|---|---|
| **완주율 (E2E 성공률)** | 시스템이 진짜 끝까지 돈다 | `kpi_measure.py` 자동측정·재현가능 |
| **평균 분석 시간** (며칠→수분) | 압도적으로 빠르다 | 자동측정 (게이트 시간 제외) |
| **자동 오류 해결률** | 스스로 고친다 | 오류 테스트 + Guardian 로그 |
| **보안 방어** | production급 안전 | 침투 테스트 (실제 테스트한 케이스 수로 표기) |

+ 보조 배지: **카테고리 4/4 커버**(완성도), **자가학습 효과(재실행 시 개선)**.

> ⚠️ **발표 전 실측 필수:** `python scripts/kpi_measure.py --since 7d` → 표의 숫자를 실측치로 교체.
> ⚠️ **부록과 일치:** v1 부록의 "KPI 11종"은 코드 정의(주요 4종 + 보조)와 맞지 않으므로 **"주요 4개 + 보조 메트릭"으로 통일**. "보안 50종"은 **실제 침투 테스트한 케이스 수**로 바꿀 것.

---

## 10. 트러블 슈팅

**이 슬라이드의 줄거리(고리):** "개발 내내 에러에 부딪혔고 → 대부분 Claude로 해결했고 → **그걸 아예 제품에 넣어** ADA가 스스로 고치게 만들었다." 우리의 고통이 곧 기능이 된 이야기.

**대표 사례 4개만 (전부 나열 금지):**

1. **데이터 누수** — ID/PK 식별자 컬럼이 학습에 섞이고 전처리 통계가 검증셋까지 새어 성능이 비현실적으로 높게 나옴 → **id-like 자동 제외 + 통계는 학습셋에만(3분할) + 누수 가드 테스트**로 차단.
2. **임베딩 불일치** — API를 여러 워커로 띄우니 워커마다 임베딩이 미묘하게 달라져 KB 검색이 들쭉날쭉 → **uvicorn 단일 워커(`--workers 1`)** 로 싱글톤 일관성 확보.
3. **CI/CD import 충돌** — 병렬 개발 중 import 정렬·미사용·중복으로 CI가 반복적으로 깨짐 → **ruff(I001·F401·F811) + pytest를 커밋/CI 게이트로 강제**.
4. **분석 속도 (8~10분 → 3분 내외)** — 분석에 시간이 오래 걸림 → **업로드 즉시 백엔드가 자동 분석 시작**(사용자가 팝업에서 선택하는 동안 백엔드는 이미 분석 중) → 단계별 체감 시간을 **3분 내외로** 단축.

**발표 멘트(고리 마무리):** "개발하는 동안 Claude API로 크고 작은 오류를 계속 해결했는데, **그 오류와 해결책을 우리 자가학습 시스템에 쌓으면서 Claude 의존도를 점점 줄였습니다.** 결국 우리의 디버깅 경험 자체가 ADA의 자동 오류복구 기능이 된 셈입니다." → 자연스럽게 11.비전으로 넘어감.

**그 외(언급만/부록):** 타겟 자동 선정 정확도(도메인 키워드·Granger), 한국어 날짜 컬럼 인식 추가, 리눅스 서버판→데스크탑판 전환으로 환경 구축 문제 해결.

---

## 11. 비전

**핵심 메시지:** "여기까지 왔고, 다음은 여기로 간다." (외부 공개용 — 눈에 보이는 로드맵 슬라이드로)

**비전 한 줄:** "전공·비전공 누구나, 데이터를 올리면 분석부터 문서화까지 끝나는 **개인용 AI 데이터 분석가**."

**향후 고도화 로드맵 (v1의 10번에 섞여 있던 내용을 이리로 이동):**

- **모델 확장** — 딥러닝 추가, 지도·비지도 학습 확대.
- **성능** — PC 성능에 맞춰 Ollama 최신화, **강화학습 실제 구현**(6번의 성능 이슈 회수).
- **품질** — 분석 속도·신뢰성 향상, 카테고리·산출물 고도화.
- **제품화** — 앱(접근성 확대), **외부 공개 서비스**로 확장.

**팁:** 5~6개 키워드를 **타임라인/로드맵 비주얼**로. 마지막에 표지 카피("…를 30분에")를 한 번 더 회수.

---

## 12. Q&A

(부록 슬라이드 대기 — **28 에이전트 전체표**, 모델 목록, **KPI 주요 4 + 보조 메트릭**, 보안 테스트 케이스 등)

---

## 13. 감사합니다!

---

## 부록. 기술 스택

- 기술 스택 이미지 1장. "이런 스택으로 만들었다" 정도만 언급(부록 느낌).
- 핵심: Python · LangGraph · FastAPI · Streamlit · PostgreSQL+pgvector · Redis · MinIO · MLflow · Docker · Claude/Ollama

---

## ✅ 발표 전 확인/수정 리스트 (v2)

- [ ] **에이전트 수 28로 통일** — 코드 `agents/stubs.py`(=28) 기준. 본문·부록·정의 슬라이드 모두 28로. "28 = 어떤 그룹 합" 1줄 메모 준비.
- [ ] **팀 인원/이름 확정** — 인원 3명 vs 역할 A~D 4개. 겸임 매핑과 B·C 실명 채우기.
- [ ] **숫자 현행화** — DB 테이블 27개(초기 24+3), 컨테이너 core 10개 기본(+선택 프로파일 최대 15).
- [ ] **KPI 실측** — `python scripts/kpi_measure.py --since 7d` → 주요 4개 실측치 반영. 부록도 "주요 4 + 보조"로 통일.
- [ ] **근거 약한 수치 정리** — "보안 50종"은 실제 침투 테스트 케이스 수로, "모델 19개"는 부록에 목록/예시로, "2회차 +5%p·trial −30%"는 실측 또는 "내부 측정 기준" 표기.
- [ ] **게이트 번호 통일** — 데모 화면 라벨과 슬라이드 6·7의 G 번호를 일치(사용자 선택 5회 기준).
- [ ] **레드 스레드 데이터 1개(타이타닉)**로 데모·슬라이드 관통.
- [ ] **강화학습 = "설계·향후"** 표기 유지(과장 금지).
