# Python: 3.10  (Dockerfile FROM python:3.10-slim 과 동일 / 호스트 venv 도 3.10 권장)
# pip 표준에는 인터프리터 버전을 핀하는 문법이 없어 주석 + .python-version 으로 명시.

# =============================================================
# api.txt  -  FastAPI 서버용
# =============================================================
-r base.txt

# 웹 프레임워크
fastapi==0.111.0
uvicorn[standard]==0.29.0
uvloop==0.19.0; sys_platform != "win32"   # Windows 미지원 — Linux/Mac 전용
python-multipart==0.0.9
aiofiles==23.2.1
sse-starlette==2.1.0     # SSE (Day18 진행률)
websockets==12.0         # WebSocket (Day18 대시보드)

# DB
sqlalchemy==2.0.30
asyncpg==0.29.0
psycopg2-binary==2.9.9   # alembic 동기 드라이버 (engine_from_config 필요)
alembic==1.13.1
pgvector==0.2.5          # pgvector Python 클라이언트 (Day19 RAG)

# KB 벡터 검색 — torch(CPU) 는 requirements/torch-cpu.txt 로 분리하여
# Dockerfile.api 가 먼저 설치한다. 여기에는 순수 PyPI 패키지만 남긴다.
# (--extra-index-url 를 전역에 두면 모든 패키지가 느린 pytorch 인덱스를 조회 → 빌드 지연)
sentence-transformers==2.7.0  # paraphrase-multilingual-mpnet-base-v2 (768dim) — torch 의존(선설치됨)

# LLM
anthropic==0.28.0        # Claude API
langgraph==0.3.34        # 그래프 resume (Day04) — CompatMemorySaver 브릿지 사용
langgraph-checkpoint-postgres==2.0.21   # HITL 체크포인터 (CompatMemorySaver 로 우회)
psycopg[binary]>=3.1                     # PostgresSaver(psycopg3) 드라이버
langchain==0.2.3
langchain-anthropic==0.1.13
langsmith==0.1.147        # 트레이싱

# 보안 (Day17)
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4              # 패스워드 해시
itsdangerous==2.2.0                 # 서명된 쿠키
authlib==1.3.1                      # OAuth2/OIDC 클라이언트 (v1 구글 로그인) — httpx(anthropic 의존)·cryptography(python-jose) 재사용
hvac==2.3.0                         # Vault 클라이언트
slowapi==0.1.9                      # rate-limit
celery==5.4.0                       # Celery 태스크 디스패치 (runner import)
redis==5.0.4                        # Celery Redis 브로커 드라이버 + publish_progress
