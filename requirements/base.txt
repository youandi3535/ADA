# Python: 3.10  (Dockerfile FROM python:3.10-slim 과 동일 / 호스트 venv 도 3.10 권장)
# pip 표준에는 인터프리터 버전을 핀하는 문법이 없어 주석 + .python-version 으로 명시.

# =============================================================
# base.txt  -  모든 컨테이너 공유
# 다른 requirements 파일들이 -r base.txt 로 상속받음
# =============================================================

pydantic==2.7.1
pydantic-settings==2.3.0
structlog==24.2.0
python-dotenv==1.0.1
httpx==0.27.0
boto3==1.34.101
tenacity==8.3.0          # 재시도 데코레이터
orjson==3.10.3           # 빠른 JSON