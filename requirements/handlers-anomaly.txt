# =============================================================
# handlers-anomaly.txt  -  이상탐지 카테고리 (NY 단독)
# =============================================================
# 본 파일은 NY (anomaly 종주) 가 단독 수정.
# .github/CODEOWNERS 에 @ny 로 강제.
#
# 현재는 비어 있음 — Day 1 NY 본구현 시 PyOD 활성화.
# scikit-learn (IsolationForest, OneClassSVM, LOF) 은 worker.txt 에 포함됨.
# =============================================================

# 이상탐지 백본 (Day 6 NY 본구현 시 활성화)
# pyod>=2.0.0,<3.0           # R-1003: PyOD 이상탐지 카테고리 백본
# alibi-detect==0.12.0       # (선택) drift / outlier 추가 백본
