# Python: 3.10  (Dockerfile FROM python:3.10-slim 과 동일 / 호스트 venv 도 3.10 권장)
# pip 표준에는 인터프리터 버전을 핀하는 문법이 없어 주석 + .python-version 으로 명시.

# =============================================================
# dev.txt  -  로컬 개발/테스트 (이미지에는 포함되지 않음)
# 호스트 가상환경에서 직접 설치
# =============================================================
-r base.txt
-r api.txt
-r worker.txt

pytest==8.2.1
pytest-asyncio==0.23.7
pytest-cov==5.0.0
pytest-mock==3.14.0
httpx==0.27.0
locust==2.27.0          # Day20 부하 테스트
ruff==0.15.15           # 린터 (pyproject [tool.ruff] required-version >=0.15.0 과 일치)
mypy==1.10.0
pre-commit==3.7.1
ipython==8.24.0
jupyterlab==4.2.1       # (선택) 로컬 노트북
